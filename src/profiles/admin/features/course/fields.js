import { fields } from '@helpers/fields'

const clazz = [fields.className, fields.source]
const course = [fields.courseName, fields.thumbnail, fields.describe, fields.categories, fields.published]
const section = [fields.sectionName]

export { clazz, course, section }
